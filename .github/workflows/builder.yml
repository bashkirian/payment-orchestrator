name: Build and Push Builder Image

on:
  push:
    branches:
      - main
      - master
    paths:
      - Dockerfile          # only rebuild when the builder definition changes
  workflow_dispatch:        # allow manual trigger (e.g. after a protoc version bump)

permissions:
  contents: read
  packages: write           # needed to push to ghcr.io

jobs:
  build-and-push:
    name: Build Builder Image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          # Tag as :latest (used by pr.yml) and as :<sha> for traceability
          tags: |
            ghcr.io/${{ github.repository_owner }}/fintech-builder:latest
            ghcr.io/${{ github.repository_owner }}/fintech-builder:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
